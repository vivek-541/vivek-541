name: Update Profile SVG

on:
  schedule:
    - cron: '30 18 * * *'
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'template.svg'
      - 'build-svg.js'

permissions:
  contents: write

jobs:
  update-svg:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
      
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: 🎨 Generate updated SVG
        env:
          OPENWEATHER_API_KEY: ${{ secrets.OPENWEATHER_API_KEY }}
        run: node build-svg.js
      
      - name: 📊 Check for changes
        id: git-check
        run: |
          git diff --exit-code chat.svg || echo "changed=true" >> $GITHUB_OUTPUT
      
      - name: 💾 Commit and push if changed
        if: steps.git-check.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add chat.svg
          git commit -m "chore: update profile with latest data [skip ci]"
          git push
      
      - name: ℹ️ Status
        run: |
          if [ "${{ steps.git-check.outputs.changed }}" == "true" ]; then
            echo "✅ Profile updated successfully!"
          else
            echo "ℹ️ No changes detected, profile is up to date."
          fi
